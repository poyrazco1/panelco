# Copy to .env and fill in real values. Never commit .env.
CASE_EIGHT_ENV=development

# Optional external narrative AI provider (see docs/SOURCE_3D_CONVERSION_PROMPT.md
# section 14.2 -- must be used server-side only, never in the client).
# Not implemented yet (docs/STATUS.md).
# NARRATIVE_PROVIDER_API_KEY=

# PostgreSQL (production only; SQLite is used for local dev by default).
# DATABASE_URL=postgresql://user:password@localhost:5432/case_eight
